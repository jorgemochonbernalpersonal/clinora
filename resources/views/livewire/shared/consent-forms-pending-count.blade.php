<div>
    @if($this->pendingCount > 0)
        <span class="px-2 py-0.5 text-xs font-semibold bg-yellow-500 text-white rounded-full min-w-[20px] text-center">
            {{ $this->pendingCount }}
        </span>
    @endif
</div>

